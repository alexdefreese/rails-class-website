%h1 New Blogpost

= form_for @blogpost do |f|
  .form-group
    = f.label :title
    = f.text_field :title
  .form-group
    = f.label :body
    = f.text_area :body
  = f.submit "Create New Blogpost", class: 'btn btn-large btn-primary'
